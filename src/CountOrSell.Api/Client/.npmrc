# Fail fast when the Node version does not satisfy "engines" in package.json.
# Without this, npm only warns, and a mismatched Node silently rewrites every
# bundle content hash in the tracked wwwroot output.
engine-strict=true
